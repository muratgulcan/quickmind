/** Mirrors GameMode.swift */
export const GameMode = Object.freeze({
  shapes: 'shapes',
  math: 'math',
  memory: 'memory',
  minMax: 'minMax',
  color: 'color',
});

export const ALL_GAME_MODES = Object.values(GameMode);
