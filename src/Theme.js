/** Theme options for settings UI */
export const Theme = Object.freeze({
  system: 'system',
  light: 'light',
  dark: 'dark',
});

export const ALL_THEMES = [Theme.system, Theme.light, Theme.dark];
